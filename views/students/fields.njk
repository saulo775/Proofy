<div class="item">
    <div>Foto de perfil</div>
    <div>
        <input type="url" name="avatar_url" placeholder="https://" value="{{students.avatar_url}}">
    </div>
</div>
<div class="item">
    <div>Nome completo</div>
    <div>
        <input type="text" name="name" placeholder="Seu nome aqui"value="{{students.name}}">
    </div>
</div>
<div class="item" >
    <div>Data de nascimento</div>
    <div>
        <input type="date" name="birth" value="{{students.birth}}">
    </div>
</div>
<div class="item" >
    <div>E-mail</div>
    <div>
        <input type="email" name="email" value="{{students.email}}" placeholder="seuemail@.com">
    </div>
</div>
<div class="item">
    <div>Ano escolar</div>
    <div>
        <select name="school_year">
            <option
                value="5F"
                {% if students.school_year == '5F'%}
                    selected
                {% endif %}
                >5º ano do ensino fundamental
            </option>
            <option
                value="6F"
                {% if students.school_year == '6º ano do ensino fundamental'%}
                    selected
                {% endif %}
                >6º ano do ensino fundamental
            </option>
            <option
                value="7F"
                {% if students.school_year == '7º ano do ensino fundamental'%}
                    selected
                {% endif %}
                >7º ano do ensino fundamental
            </option>
            <option
                value="8F"
                {% if students.school_year == '8º ano do ensino fundamental'%}
                    selected
                {% endif %}
                >8º ano do ensino fundamental
            </option>
            <option
                value="1M"
                {% if students.school_year == '1º ano do ensino medio'%}
                    selected
                {% endif %}
                >1º ano do ensino medio
            </option>
            <option
                value="2M"
                {% if students.school_year == '2º ano do ensino medio'%}
                    selected
                {% endif %}
                >2º ano do ensino medio
            </option>
            <option
                value="3M"
                {% if students.school_year == "3º ano do ensino medio" %}
                    selected
                {% endif %}
                >3º ano do ensino medio
            </option>

        </select>
    </div>
</div>

<div class="item ">
    <div>Carga horária semanal</div>
    <div>
        <input
            type="number"
            name="workload"
            placeholder="quantas horas de aulas o aluno tera por semana?"
            value="{{students.workload}}"
            min='1' max='48'>
    </div>
</div>

<button type="submit">Salvar</button>